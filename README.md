Чтобы скомпилировать необходимо запустить скрипт build_cmake.sh
Для того чтобы данные были float - необходимо запускать ```bash build_cmake.sh float```
Аналогично для типа double  ```bash build_cmake.sh double```
